import type { NextPage } from 'next';
import Head from 'next/head';

const Home: NextPage = () => (
  <Head>
    <title>Physics Tools</title>
    <meta
      name="description"
      content="Web page with tools to understand concepts of maths and physics"
    />
    <link rel="icon" href="/favicon.ico" />
  </Head>
);

export default Home;
